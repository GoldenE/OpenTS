add_executable(HDAssetsTest main.cpp "${CMAKE_SOURCE_DIR}/code/hdasset.cpp")
target_include_directories(HDAssetsTest PRIVATE "${CMAKE_SOURCE_DIR}/code")
target_compile_features(HDAssetsTest PRIVATE cxx_std_20)
target_compile_options(HDAssetsTest PRIVATE /EHsc)
target_link_libraries(HDAssetsTest PRIVATE bcrypt)
add_test(NAME hdassets COMMAND HDAssetsTest)

set(runtime_path "${CMAKE_SOURCE_DIR}/code/hdruntime.cpp")
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${runtime_path}")
file(READ "${runtime_path}" runtime_source)
string(REGEX REPLACE "#include[^\n]*\n" "" runtime_source "${runtime_source}")
file(CONFIGURE OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/hdruntime_under_test.inc" CONTENT "${runtime_source}" @ONLY)
add_executable(HDRuntimeTest runtime.cpp "${CMAKE_SOURCE_DIR}/code/hdasset.cpp" "${CMAKE_SOURCE_DIR}/code/_hdruntime.cpp")
target_include_directories(HDRuntimeTest PRIVATE "${CMAKE_CURRENT_BINARY_DIR}" "${CMAKE_SOURCE_DIR}/code")
target_compile_features(HDRuntimeTest PRIVATE cxx_std_20)
target_compile_options(HDRuntimeTest PRIVATE /EHsc)
target_link_libraries(HDRuntimeTest PRIVATE bcrypt)
add_test(NAME hdruntime COMMAND HDRuntimeTest)

set(shape_path "${CMAKE_SOURCE_DIR}/code/hdshape.cpp")
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${shape_path}")
file(READ "${shape_path}" shape_source)
string(REGEX REPLACE "#include[^\n]*\n" "" shape_source "${shape_source}")
file(CONFIGURE OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/hdshape_under_test.inc" CONTENT "${shape_source}" @ONLY)
add_executable(HDShapeTest shape.cpp "${CMAKE_SOURCE_DIR}/code/hdasset.cpp")
target_include_directories(HDShapeTest PRIVATE "${CMAKE_CURRENT_BINARY_DIR}" "${CMAKE_SOURCE_DIR}/code")
target_compile_features(HDShapeTest PRIVATE cxx_std_20)
target_compile_options(HDShapeTest PRIVATE /EHsc)
target_link_libraries(HDShapeTest PRIVATE bcrypt)
add_test(NAME hdshape COMMAND HDShapeTest)

find_package(Python3 3.10 COMPONENTS Interpreter QUIET)
if(Python3_Interpreter_FOUND)
    add_test(NAME hdpack COMMAND "${Python3_EXECUTABLE}" "${CMAKE_SOURCE_DIR}/tools/hdpack/test_hdpack.py" "$<TARGET_FILE:HDAssetsTest>")
endif()
